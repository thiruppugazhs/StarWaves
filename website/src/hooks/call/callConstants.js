/** Call constants — single responsibility: timing and busy phase definitions. */

export const RING_TIMEOUT_MS = 35000
export const ECHO_COOLDOWN_MS = 700

export const BUSY_PHASES = ['dialing', 'connecting', 'active', 'incoming']
